# ==============================================================================
# FEATURE FLAGS & CAPABILITY ROLLOUT
# ==============================================================================
# Track toggles, percentage rollouts, and deprecation dates.
# Agents must consult this before shipping gated behavior.

version: "2.0.0"
last_updated: "2026-08-08"

features:
  - key: "example_feature"
    name: "Example Feature"
    description: "Placeholder flag — replace or remove when adding real capabilities."
    enabled: false
    percentage_rollout: 0
    environments:
      development: true
      staging: false
      production: false
    owner: "{{AUTHOR_EMAIL}}"
    deprecation_date: null
    related_spec: null
